rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 個人利用: sectors/latest ドキュメントのみ読み書き許可
    // 本番運用時は allow write: if request.auth != null; などに変更推奨
    match /sectors/latest {
      allow read, write: if true;
    }
  }
}
